SEV_decommunization = {

    SEV_rename_capital = {
    
        icon = anti_communist

        available = {
            NOT = { country_exists = STG }
            153 = { is_owned_and_controlled_by = ROOT }
        }
        
        visible = {
            NOT = { country_exists = STG }
            153 = { is_owned_and_controlled_by = ROOT }
        }
        
        allowed = {
            tag = SEV
        }

        cost = 30
        fire_only_once = yes

        ai_will_do = {
            factor = 200
        }
		
		days_remove = 14

        remove_effect = {
            SEV = { 
				country_event = {	
					id = severf.0
					days = 1
				}
			}
		}
    }
	SEV_rename_rest_of_country = {
    
        icon = anti_communist

        available = {
            NOT = { country_exists = STG }
            153 = { is_owned_and_controlled_by = ROOT }
        }
        
        visible = {
            NOT = { country_exists = STG }
            153 = { is_owned_and_controlled_by = ROOT }
        }
        
        allowed = {
            tag = SEV
        }

        cost = 50
		
        fire_only_once = yes

        ai_will_do = {
            factor = 200
        }
		
		days_remove = 20

        remove_effect = {
			165 = { set_state_name = "Legkoputnaya Village" }
			161 = { set_state_name = "Wheat Farmlands" }
			235 = { set_state_name = "Khorsk" }
			332 = { set_state_name = "Maremansk" }
			334 = { set_state_name = "Twierdza" }
			set_province_name = { id = 2082 name = "Poniwiec" }
			add_political_power = 100
        }
    }
	SEV_remove_army_legacy = {
    
        icon = commie_army_decision

        available = {
            NOT = { country_exists = STG }
            has_idea = SEV_communist_army_legacy
        }
        
        visible = {
            NOT = { country_exists = STG }
            has_idea = SEV_communist_army_legacy
        }
        
        allowed = {
            tag = SEV
        }

        cost = 50
		
        fire_only_once = yes

        ai_will_do = {
            factor = 200
        }
		
		days_remove = 35

        remove_effect = {
			remove_ideas = SEV_communist_army_legacy
        }
    }
	SEV_remove_economic_legacy = {
    
        icon = anti_communist

        available = {
            NOT = { country_exists = STG }
            has_idea = SEV_commununist_economy_legacy
        }
        
        visible = {
            NOT = { country_exists = STG }
            has_idea = SEV_commununist_economy_legacy
        }
        
        allowed = {
            tag = SEV
        }

        cost = 50
		
        fire_only_once = yes

        ai_will_do = {
            factor = 200
        }
		
		days_remove = 35

        remove_effect = {
			remove_ideas = SEV_commununist_economy_legacy
        }
    }
	SEV_remove_political_legacy = {
    
        icon = anti_communist

        available = {
            NOT = { country_exists = STG }
            has_idea = SEV_communist_political_legacy
        }
        
        visible = {
            NOT = { country_exists = STG }
            has_idea = SEV_communist_political_legacy
        }
        
        allowed = {
            tag = SEV
        }

        cost = 50
		
        fire_only_once = yes

        ai_will_do = {
            factor = 200
        }
		
		days_remove = 35

        remove_effect = {
			remove_ideas = SEV_communist_political_legacy
        }
    }
}